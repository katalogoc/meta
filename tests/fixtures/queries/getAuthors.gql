query getAuthors($options: QueryOptions) {
  authors(options: $options) {
    id
    birthdate
    deathdate
    name
    alias
    thumbnail
    texts {
      id
      title
      url
      subject
    }
  }
}
