query getText($id: ID!) {
  text(id: $id) {
    id
    xid
    source
    title
    url
    subject
    authors {
      id
      xid
      source
      name
      birthdate
      deathdate
      thumbnail
      alias
    }
  }
}
