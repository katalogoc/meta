overwrite: true
schema: "src/schema.graphql"
documents: null
generates:
  src/common/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-document-nodes"
